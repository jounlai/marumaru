# ことばの氷山 — 素材の指示書（Codex 向け）

〇〇ことば（https://marumaru.heuron.com/）のステージを、日本の都市巡りから
**ことばの氷山**に置き換えた（commit `64a0cd7`）。コードは入れ終わっていて、
足りないのは**絵**だけ。この文書は、その絵の仕様書です。

## 0. なぜ氷山なのか（絵を決めるときの拠り所）

このゲームのラウンドは **正解数の多い順** に並んでいる。

| ステージ | 例 | 正解数 |
|---|---|---:|
| 序盤 | `〇ん〇ん`（かんかん・ぷんぷん…） | 60語 |
| 終盤 | `〇にぐ` など | 4語 |

つまり**進むほど正解が減り、見慣れない語ばかりになる**。これは
「水の上は誰でも知っている・下ほど数が減って奇妙になる」という氷山の形そのもの。
前は札幌→那覇の旅にしていたが、**札幌と那覇のあいだに難しくなる理由が無く**、
移動が飾りに見えていた。深さにすると、並び順がそのまま物語になる。

**だから、絵は「深さ＝難しさ」を伝えるものでなければならない。**
きれいな海の絵ではなく、*下へ行くほど心細くなる*絵が要る。

## 1. いまあるもの

```
img/maru-think.png  332x360   考える
img/maru-run.png    389x360   走る（← いまはこれを90度倒して「潜る」にしている）
img/maru-pose.png   343x360   決めポーズ
img/maru-good.png   383x360   親指を立てる
img/logo.png / logo-small.png / logo-hero.png    ロゴ（変えない）
img/ogp.png         1200x630  SNSカード
img/icon-32.png / icon-180.png                   アイコン（変えない）
```

キャラクター名は **まるくん**。白い円盤の頭に赤い「の」、白い体、赤いマント。
**線は黒ではなく濃紺**。既存4枚と同じ絵柄・同じ線の太さで描くこと。

| 役 | 色 |
|---|---|
| 体（白） | `#ebecf1` / 影 `#b0b4ce` |
| 輪郭線 | `#131e54` |
| マント | `#ed0f1e` / 影 `#9e0108`・`#710206` / ハイライト `#fa545e` |

水の色（CSS で使っている値。絵もこれに合わせる）：

```
水の上 #6fb7e8 → 水面ぎわ #a9dcf8 → #2b7fb8 → #0f5280 → #072f4f → #03172a → 底 #000
金（CLEAR/PERFECT） #ffd34d
こども版の6色 #ffd42a #ff8a1f #38a9ea #8ac93a #f2789f #fff3cf
```

## 2. 作ってほしいもの（優先順）

### A（最優先）氷山そのもの — `img/iceberg.png`

いまステージ一覧の背景は **CSS のグラデーションだけ**で、氷山の形が無い。
これが入るだけで、premise の伝わり方がいちばん変わる。

- **480 × 2400px、透過PNG**（縦長。CSS が `background-size:100% 100%` で伸ばす）
- **画像の上から 16% の高さが水面**。そこより上が水面上に出た小さな峰、
  下が水中の巨大な塊。上下の比は **1 : 5 以上**（「氷山の一角」が一目で分かること）
- 塊は下へ行くほど細く・暗くなり、**画像の下端では溶けて見えなくなる**
  （底が描かれていると「どこまで潜れるか分からない」感じが消える）
- 色は氷の白青（`#dff1ff` 〜 `#4a86ad`）。**背景のグラデーションに重ねるので、
  全体の不透明度は 35〜45% を想定した薄さで描く**
- 亀裂・気泡の筋を入れてよい。写真ではなく、ロゴと同じ**平面的な塗り**で
- 幅方向は中央寄せ。左から 90〜100px の位置に停留所の丸が縦に並ぶので、
  **その帯には濃い模様を置かない**（数字が読めなくなる）

> 縦横比が合わないときは引き伸ばされる。ステージ数はモードで違い、
> 実際の描画高は 900〜1000px 程度になる。有機的な形なので多少伸びても構わないが、
> **水面の 16% だけは動かさないこと**。

### B 潜るまるくん — `img/maru-dive.png`

いまは `maru-run.png` を CSS で90度倒して代用している。走る足のままなので、
よく見ると走っている。

- **360px 高さ・透過PNG**（既存4枚と同じ）、幅は成り行き
- **頭を下にして、まっすぐ潜っていく姿**。両腕は前（＝下）へ伸ばす
- マントは**上へ流れる**（落ちているので）
- 顔は息を止めた顔。水中メガネを掛けてよい
- 使う場所では **58px / 32px** まで縮む。細かい描き込みは効かない

### C 深度で変わるまるくん（おとな版） — `img/maru-dive-1.png` 〜 `-4.png`

**この企画のいちばん面白いところ。** 潜るほど探索者が変わっていく、という
氷山ミームの笑いをそのまま使う。B はこの `-1` を兼ねてよい。

| 枚 | 深さ | ステージ | 姿 |
|---|---|---|---|
| `-1` | +40m 〜 −15m | 1–5 | ふつう。余裕がある。水中メガネくらい |
| `-2` | −40m 〜 −420m | 6–10 | 少し必死。頬がこけ始める。マントがくたびれる |
| `-3` | −650m 〜 −2,700m | 11–16 | 目が据わる。無精ひげ。マントが破れる |
| `-4` | −3,400m 以深 | 17– | 限界。目が光る。ぼろぼろ。もう笑っていない |

- **顔の変化を主に、シルエットは変えない**（32pxでも違いが分かるように、
  変化は「頭の周り」＝ひげ・目・髪に集めること）
- 怖くしすぎない。**あくまで笑い**。ホラーにはしない
- 4枚とも**同じ姿勢・同じ大きさ・同じ位置**で描く（差し替えても跳ねないように）

### D こども版の潜るまるくん — `img/maru-kids-1.png` 〜 `-4.png`

こども版に C は出さない（やつれていく絵は出さない）。**装備が増えていく**方向で、
最後まで笑顔にする。

| 枚 | 姿 |
|---|---|
| `-1` | 浮き輪 |
| `-2` | 水中メガネとシュノーケル |
| `-3` | 潜水服（丸いヘルメット） |
| `-4` | 小さな潜水艇に乗っている |

- こども版は文字も絵も大きい。**色は明るく、輪郭は太く**
- `-3` `-4` は余力があれば。無ければ `-2` を使い回してよい

### E SNSカードの作り直し — `img/ogp.png`

- **1200 × 630px**。いまはロゴだけで、何のゲームか伝わらない
- 左に `〇んこう` のような**お題を大きく**、右に氷山とまるくん、
  下に小さくロゴ。X のタイムラインで縮んでも**お題が読めること**が最優先
- 既存の `img/ogp.png` を置き換える（ファイル名は変えない）

### F（任意）移動演出の壁 — `img/dive-wall.png`

層から層へ沈むときの背景に、氷の壁が横を流れていくと深さが出る。
**240 × 900px・透過PNG・上下がつながる（タイル可能な）氷の断面**。
無くても成立するので、A〜E が終わってから。

## 3. 共通の決まり

- すべて **透過PNG**。JPEG・WebP は使わない（対応表を増やしたくないため）
- 1枚 **40KB以下**（既存のキャラは8〜12KB、ogp は210KB）。
  `pngquant` などで減色してよいが、輪郭のジャギは残さない
- ファイル名は上記のとおり。**既存ファイルを別名に変えない**
  （`index.html` と `css/styles.css` の両方から参照されている）
- 生成した画像に**文字を焼き込まない**（`ogp.png` を除く）。
  文言は HTML 側で出しており、こども版とおとな版で違う

## 4. 組み込み方

素材を置くだけでは表示されない。以下も同じ変更に含めること。

### A 氷山

`css/styles.css` の `.stripInner`（「ステージ選び＝ことばの氷山」の節）に重ねる。
**いまのグラデーションは消さないこと**（画像が落ちても水の濃さで深さが伝わるため）。

```css
.stripInner{
  /* いまの linear-gradient(...) はそのまま残し、その手前に敷く */
  background-image:url("../img/iceberg.png"),linear-gradient(180deg, ...既存... );
  background-size:100% 100%,100% 100%;
  background-repeat:no-repeat,no-repeat;
}
```
不透明度は画像側で持たせる（`opacity` を掛けると上の文字まで薄くなる）。

### B〜D まるくん

差し替え先は2か所。

| 場所 | いまの指定 | ファイル |
|---|---|---|
| ステージ一覧の現在地 | `<img class="stopChar" src="img/maru-run.png">` | `js/game.js` の `openRoundList()` |
| 沈む演出 | `<img class="tvChar" src="img/maru-run.png">` | `index.html` の `#travel` |

深さで替えるので、**JS 側に1つ関数を足して両方から呼ぶ**こと。

```js
// 潜るほど、まるくんが変わる。深さが数字だけでなく姿にも出るように。
function diverImg(si){
  const t = si < 5 ? 1 : si < 10 ? 2 : si < 16 ? 3 : 4;
  return `img/maru-${mode === "kids" ? "kids" : "dive"}-${t}.png`;
}
```

`#travel` の `<img class="tvChar">` は静的に書かれているので、`travelTo()` の中で
`$(".tvChar").src = diverImg(to)` を入れる（**着く先**の深さで替える）。

`css/styles.css` の `.tvChar` にある `rotate(90deg)` と、`@keyframes tvDive` の
`rotate(74deg 〜 99deg)` は、**正しい潜る絵に差し替えたら 0度前後に直すこと**
（走る絵を倒すための応急処置なので）。揺れは残してよい：`rotate(-8deg 〜 8deg)`。

### 最後に必ず

CSS/JS を触ったらキャッシュ破りの版を上げる。忘れると**すでに遊んだ人のブラウザが
古いファイルを使い続ける**。

```bash
node tests/lint-data.js          # ?v=xxxxxxxx を出す
# index.html の ?v= をその値に一括置換
node tests/lint-data.js && node tests/smoke.js && node tests/deadend.js
```

## 5. 受け入れ条件

- [ ] 上の3つのテストが通る（`smoke.js` の `window error: ... 'catch'` は既知・無害）
- [ ] `index.html` の `?v=` が `lint-data.js` の出す値と一致している
- [ ] ステージ一覧を開くと、水面の白い線と**氷山の峰の位置が合っている**
- [ ] 一覧を下までスクロールしても、停留所の数字と層の名前が読める
- [ ] 深いステージを選ぶと、まるくんの姿が変わる（おとな版・こども版の両方）
- [ ] 沈む演出でまるくんが**頭から**落ちていく（走っていない）
- [ ] iPhone SE（375×667）で一覧が横にあふれない
- [ ] こども版に、やつれたまるくん（C）が出ない

確認用のスクリーンショットは `node tests/shot.js`（`tests/shots/` に出る）。

## 6. やらないこと

- ロゴ・アイコン・ヘッダーは変えない（`ogp.png` だけ例外）
- 出題データ（`js/data.js`）には触れない
- 依存パッケージを増やさない。**このアプリは `index.html` を開けば動く**のが前提で、
  ビルド工程を持たない
- 音は足さない（WebAudio で合成しており、音声ファイルを持たない方針）
- 写真素材・外部CDN・Webフォントを使わない
